You are my front-end developer. Build me a personal portfolio website with a
CREATIVE CODER / DIGITAL ARTIST theme. I am a high school student who makes generative art
and interactive web experiments.

ABOUT ME (placeholder content I can edit later):
- Name: Sanjna Maduri
- Tagline: creative coder / digital artist + "I make art with code"
- City: YOUR CITY
- Bio: how I got into creative coding (p5.js, etc.) and what I love about it.
- 4 project/experiment cards, each with: title, one-line description, and tags
  (p5.js, Three.js, Canvas, Web Audio, etc.). Use colorful gradient thumbnails as placeholders.
- A scrolling marquee of tools I use.
- Social links: email, GitHub, LinkedIn, and Instagram (placeholder URLs).

TECH REQUIREMENTS (important):
- Deliver ONE single self-contained file named index.html (all CSS in <style>, all JS in
  <script>, no build tools, no frameworks, no external libraries). It must open by double-clicking.
- Google Fonts via CDN <link> is fine, with readable fallbacks offline.
- Plain HTML, CSS, and vanilla JavaScript only. The animation must use the built-in HTML
  <canvas> element and requestAnimationFrame — do NOT load p5.js or any library.

DESIGN:
- Bold, playful, colorful aesthetic. Dark by default with a vivid accent (orange/red),
  plus a couple of secondary colors used in gradients.
- A heavy display font for headings (e.g. Bricolage Grotesque) + Space Grotesk for body.
- A FULL-SCREEN HERO with a live, interactive particle network drawn on a <canvas>: floating
  dots connected by lines, where the lines fade with distance and the particles gently push
  away from the mouse cursor. The hero text sits centered on top of it. The particle color
  should follow my chosen accent color. Keep it lightweight and smooth.
- An animated morphing "blob" shape as the about-section photo placeholder.
- Project cards in a responsive grid with gradient thumbnails that lift slightly on hover.
- Sections: full-screen Hero (canvas), About, Work (card grid), Skills (marquee), Contact
  (giant headline). Fixed/floating top nav.

MUST-HAVE FEATURES:
- Dark / light mode toggle that remembers my choice in localStorage.
- A row of accent-color swatches in the nav that recolors the site AND the particle animation,
  saved to localStorage.
- Fully responsive: the canvas resizes to the screen and the number of particles scales down
  on small screens for performance. Everything looks great on phone, tablet, and laptop with
  no overflow or overlap. Use clamp() for fonts.
- Photo placeholder (the blob) with a dashed outline and a label.
- HTML comments marked [EDIT] everywhere I should customize text.

ALSO CREATE README.md for a beginner explaining:
1. opening index.html, 2. editing text/colors/links, 3. swapping the blob for my photo,
4. how the canvas animation works at a high level, 5. publishing free with GitHub Pages
   AND Netlify (drag-and-drop).

After creating the files, tell me in one paragraph what to edit first.