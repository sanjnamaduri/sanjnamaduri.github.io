You are my front-end developer. Build me a clean, MINIMALIST personal portfolio website.
I am a high school student. The vibe should be calm, editorial, and professional — the kind
of site that looks good on a college or internship application.

ABOUT ME (placeholder content I can edit later):
- Name: YOUR NAME
- Tagline: a high school student who likes to build useful things
- City: YOUR CITY
- Focus areas (e.g. CS, Design) and current status (e.g. open to internships)
- A short, sincere "About" paragraph.
- 3 projects, each with: title, year, and a one-line description.
- A list of skills as simple pills/tags.
- Social links: email, LinkedIn, and GitHub (placeholder URLs).

TECH REQUIREMENTS (important):
- Deliver ONE single self-contained file named index.html (all CSS in <style>, all JS in
  <script>, no build tools, no frameworks). It must open by double-clicking.
- Google Fonts via CDN <link> is fine, with readable fallbacks for offline.
- Plain HTML, CSS, and vanilla JavaScript only.

DESIGN:
- LIGHT, airy aesthetic by default with lots of whitespace and one tasteful accent color.
- A refined serif for headings (e.g. Newsreader) + a clean sans for body (e.g. Inter Tight).
- Narrow, centered single-column reading width (~760px max) so it feels like a nice document.
- Hero with a circular monogram avatar (my initials) that I can later replace with a photo,
  a big confident headline, a short intro, and a small meta row (location / focus / status).
- Sections laid out as label-on-the-left, content-on-the-right rows separated by thin lines:
  About, Selected Work (as an understated list of links), Skills (tags), Contact (a tidy list
  of social rows). Sticky minimal top nav.

MUST-HAVE FEATURES:
- Dark / light mode toggle that remembers my choice in localStorage (default light).
- A small row of accent-color swatches in the nav to recolor the site, saved to localStorage.
- Fully responsive: flawless on phone, tablet, and laptop. The left-label rows should collapse
  to single column on mobile. Nothing overflows, overlaps, or gets cut off. Use clamp() for fonts.
- HTML comments marked [EDIT] everywhere I should customize text.

ALSO CREATE README.md for a beginner explaining:
1. opening index.html, 2. editing text/colors/links, 3. replacing the monogram with a photo,
4. publishing free with GitHub Pages AND Netlify (drag-and-drop).

After creating the files, tell me in one paragraph what to edit first.