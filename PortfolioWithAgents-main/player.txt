You are my front-end developer. Build me a personal portfolio website with a
GAME DEVELOPER / GAMER theme. I am a high school student.

ABOUT ME (placeholder content I can edit later):
- Name: YOUR NAME
- Tagline: "I build games" + indie game developer
- City: YOUR CITY
- Bio: how I got into making games and what kind of games I make.
- 3 game projects, each with: title, one-line description, and tags
  (engine like Unity/Godot, language, and a label like "Jam winner").
- A few headline stats (games shipped, jam wins, total downloads, engines).
- Skills shown as a "skill tree" with labeled progress bars (e.g. Unity Lv.9 at 90%).
- Social links: email, GitHub, LinkedIn, and itch.io (placeholder URLs).

TECH REQUIREMENTS (important):
- Deliver ONE single self-contained file named index.html (all CSS in <style>, all JS in
  <script>, no build tools, no frameworks). It must open by double-clicking.
- Google Fonts via CDN <link> is fine, but stay readable offline with fallbacks.
- Plain HTML, CSS, and vanilla JavaScript only.

DESIGN:
- Dark "neon arcade" aesthetic by default with a glowing green/cyan accent.
- Monospace body font (e.g. JetBrains Mono) + bold sans display (e.g. Space Grotesk).
- Hero with a glowing headline, an "AVAILABLE FOR GAME JAMS" status badge, and a HUD-style
  row of stat cells. A subtle dotted grid background.
- Game projects as a responsive grid of cards, each with a striped placeholder "screenshot"
  area, title, description, and tags.
- Sections: Hero, About (with a square avatar placeholder), Games/Projects, Skills (XP bars),
  Contact. Sticky top nav linking to each.

MUST-HAVE FEATURES:
- Dark / light mode toggle that remembers my choice in localStorage.
- A row of accent-color swatches in the nav to recolor the whole site, saved to localStorage.
- Fully responsive: perfect on phone, tablet, and laptop. Nothing should overflow, overlap,
  or get cut off. Use clamp() for fonts and CSS grid with minmax/auto-fit so cards reflow.
- Photo placeholder with a dashed box and a label telling me what to drop in.
- HTML comments marked [EDIT] everywhere I should customize text.

ALSO CREATE README.md for a beginner explaining:
1. opening index.html, 2. editing text/colors/links, 3. adding my photo and game screenshots,
4. publishing free with GitHub Pages AND Netlify (drag-and-drop).

After creating the files, tell me in one paragraph what to edit first.